version: "2.1"
services:
  grocy:
    image: lscr.io/linuxserver/grocy:latest
    container_name: grocy
    environment:
      - PUID=1000
      - PGID=1000
      - TZ=${TZ}
    volumes:
      - grocy_data:/config
    ports:
      - ${PORT}:80
    restart: unless-stopped

volumes:
  grocy_data: